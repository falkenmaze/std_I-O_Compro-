Find the *n*-th Fibonacci number, if the set if Fibonacci series start with *f₀ = f₁ = 1* and for each *i ≥ 2*, *fᵢ = fᵢ₋₁ + fᵢ₋₂*.

**Input**
The first line contains the number of tests cases you have to solve, *T*.
The only line of each test contains a single integer n (1 ≤ n ≤ 20).

**Output**
Print the *n*-th Fibonacci number.

**Examples**
__Example 1__
Input:
```
3
2
4
```
Output:
```
2
```
__Example 2__
Input:
```
1
1
```
Output:
```
1
```